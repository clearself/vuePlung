
export default {
  // //获取公司监管等级编号
  // get_regulator_level({
  //   commit,
  //   state
  // }) {
  //   common_regulator_level().then((data) => {
  //     commit('regulator_level', data);
  //   }, (msg) => {}).catch((error) => {
  //     console.log('ajax_regulator_level' + error);
  //   });
  // },
}
